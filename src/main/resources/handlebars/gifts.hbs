<div>
    <h2>Gifts</h2>
    {{#each gifts}}
    <div>
        <h4>{{group.loreName}}</h4>
        <p>
            {{#each powers}}
            <span>@Item[{{id}}]{ {{~name~}} } </span>
            {{/each}}
        </p>
    </div>
    {{/each}}
</div>