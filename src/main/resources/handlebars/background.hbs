<div>
    {{#each (split description)}}
    <p>{{.}}</p>
    {{/each}}
</div>
{{#with ratings}}
<div>
    <table class="backgrounds">
        {{#if header}}
        <tr>
            {{#each header.columns}}
                <th>{{data}}</th>
            {{/each}}
        </tr>
        {{/if}}
        {{#each rows}}
        <tr>
            {{#each columns}}
                <td>{{data}}</td>
            {{/each}}
        </tr>
        {{/each}}
    </table>
</div>
{{/with}}
{{#with ratings.trailer}}
<div>
    <p>{{.}}</p>
</div>
{{/with}}